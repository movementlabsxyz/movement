services:

  setup:
    environment:
      MVT_RPC_CONNECTION_PROTOCOL: "https"
      MVT_RPC_CONNECTION_HOSTNAME: ${ENVIRONMENT-testnet}.bardock.movementnetwork.xyz
      MVT_RPC_CONNECTION_PORT: 443

  bridge-service:
    environment:
      MVT_RPC_CONNECTION_PROTOCOL: "https"
      MVT_RPC_CONNECTION_HOSTNAME: ${ENVIRONMENT-testnet}.bardock.movementnetwork.xyz
      MVT_RPC_CONNECTION_PORT: 443