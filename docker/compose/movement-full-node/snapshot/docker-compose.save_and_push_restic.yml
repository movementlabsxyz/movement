services:

  movement-backup-db:
    image: ghcr.io/movementlabsxyz/movement-full-node:${CONTAINER_REV}
    container_name: movement-backup-db
    environment:
      - AWS_REGION=${AWS_REGION}
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - RESTIC_PASSWORD=${RESTIC_PASSWORD}
      - SYNC_BUCKET=${SYNC_BUCKET}
    command:-r s3:s3.${AWS_REGION}.amazonaws.com/${SYNC_BUCKET}/restic_node_backup backup .movement/maptos .movement/maptos-storage .movement/movement-da-db .movement/default_signer_address_whitelist  
    volumes:
      - ${DOT_MOVEMENT_PATH}:/.movement