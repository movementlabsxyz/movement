version: "3"

environment:

processes:
  test-ggp-gas-fund-fee:
    command: |
      cargo run --bin movement-tests-e2e-ggp-fund-fee
    depends_on:
      movement-full-node:
        condition: process_healthy
      movement-faucet:
        condition: process_healthy
