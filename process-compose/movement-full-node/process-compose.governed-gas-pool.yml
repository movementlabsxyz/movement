version: "3"

environment:

processes:
  test-governed-gas-pool:
    command: |
      cargo run --bin movement-tests-e2e-governed-gas-pool
    depends_on:
      movement-full-node:
        condition: process_healthy
      movement-faucet:
        condition: process_healthy
