version: "3"

environment:

processes:
  test-lock-mint:
    command: |
      cargo run --bin movement-tests-e2e-lock-mint
    depends_on:
      movement-full-node:
        condition: process_healthy
      movement-faucet:
        condition: process_healthy