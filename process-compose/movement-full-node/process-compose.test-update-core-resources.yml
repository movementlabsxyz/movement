version: "3"

environment:

processes:
  test-update-core-resources:
    command: |
      cargo run --bin movement-tests-e2e-update-core-resources
    depends_on:
      movement-full-node:
        condition: process_healthy
      movement-faucet:
        condition: process_healthy
