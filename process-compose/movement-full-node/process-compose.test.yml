version: "3"

environment:

processes:

  movement-tests-tests:
    command: |
      cargo test -p movement-tests test_example_
    depends_on:
      movement-full-node:
        condition: process_healthy
      movement-faucet:
        condition: process_healthy
    availability:
      exit_on_end: true
