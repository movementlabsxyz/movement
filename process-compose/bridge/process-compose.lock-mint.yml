version: "3"

environment:

processes:
  lock-mint-test:
    command: |
      cargo test -p bridge-integration-tests test_lock_mint -- --nocapture --test-threads=1
    depends_on:
      bridge:
        condition: process_healthy
    availability:
      exit_on_end: true